In this example, a single client launches three echo servers. Each server echoes a message with the prefix "ECHO ====" prepended to the client.

This example is a simplification of raw-echo, using the msg package. 